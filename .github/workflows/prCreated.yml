name: "Test pr opened"
on:
  pull_request:
    types: [opened]

jobs:
  execute:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - run: npm install
      - run: npm run build
      - uses: ./
        with:
          jobs: 'pr-labeler'
          bot-token: "${{ secrets.PROW_BOT_TOKEN }}"
